# Benchmark charts

Charts and raw numbers from Qwrk's benchmark runs, referenced by issues. Nothing here is part of the library.

- `2026-09-26/`: js-framework-benchmark (Qwrk, vanilla JS, Solid 1.9.3, Svelte 5.42.1, React 19.2.0) and Lighthouse startup runs. `data.json` holds every number the charts show.

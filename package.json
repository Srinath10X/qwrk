{
  "name": "qwrk",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "prepare": "husky",
    "prepare-templates": "rm -rf templates && cp -r packages/qwrk-app/templates ./templates"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^9.1.7",
    "typescript": "^5.8.3"
  }
}

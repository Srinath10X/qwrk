# Astro + Qwrk

Mounts a Qwrk component inside an Astro page. `qwrk-vite` is added to Astro's Vite config, and the page imports the component from a bundled `<script>`.

```sh
pnpm install
pnpm dev
```

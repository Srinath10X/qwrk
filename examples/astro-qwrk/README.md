# Astro + Qwrk

Mounts a Qwrk component inside an Astro page. `qwrk-vite` is added to Astro's Vite config, components live in `src/components/`, and pages mount them from a client-side `<script>`.

```sh
pnpm install
pnpm dev
```
